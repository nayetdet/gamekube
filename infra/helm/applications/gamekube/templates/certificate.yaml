apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gamekube-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: gamekube-tls
  issuerRef:
    name: gamekube-ca
    kind: ClusterIssuer
  dnsNames:
    - gamekube.localhost
    - '*.gamekube.localhost'
