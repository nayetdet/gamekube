apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gamekube-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: gamekube-tls
  issuerRef:
    name: gamekube-selfsigned
    kind: ClusterIssuer
  dnsNames:
    - gamekube.localhost
    - '*.gamekube.localhost'
