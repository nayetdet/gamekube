redis:
  architecture: standalone
  auth:
    enabled: true
    password: redis
  master:
    service:
      type: NodePort
      nodePorts:
        redis: 30379
    persistence:
      enabled: true
      size: 256Mi
