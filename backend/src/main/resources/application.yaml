spring:
  application:
    name: gamekube

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

game:
  namespace: gamekube
  domain: gamekube.localhost
  tls-secret: gamekube-tls
  protocol: https
  readiness-timeout-seconds: 120
